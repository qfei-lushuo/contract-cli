interface:
  display_name: "Contract CLI MDM Vendor"
  short_description: "List vendor candidates and fetch vendor details with user or app identity"
  default_prompt: "Use $contract-cli-mdm-vendor to query vendor candidates or vendor details from contract-cli. Choose user or app according to the active profile and requested route."

policy:
  allow_implicit_invocation: true
