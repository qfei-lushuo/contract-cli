interface:
  display_name: "Contract CLI MDM Vendor"
  short_description: "Query vendor candidates/details and create or update vendors"
  default_prompt: "Use $contract-cli-mdm-vendor to query vendor candidates or vendor details with user or app identity, or create, update, list all, and query by certificate with app identity."

policy:
  allow_implicit_invocation: true
