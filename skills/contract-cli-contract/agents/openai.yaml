interface:
  display_name: "Contract CLI Contract"
  short_description: "Run contract commands with identity-aware field references"
  default_prompt: "Use $contract-cli-contract to select the correct user MCP, app V1, or app V2 contract search contract and run contract get/create/template/file/action/esign/authorization/cooperation commands from bundled references."

policy:
  allow_implicit_invocation: true
