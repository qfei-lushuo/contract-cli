interface:
  display_name: "Contract CLI Shared"
  short_description: "Choose the right contract-cli open platform module and follow shared command rules"
  default_prompt: "Use $contract-cli-shared to choose between contract, payment, mdm, event, and rule skills, then follow shared identity, input, output, and api-call availability conventions."

policy:
  allow_implicit_invocation: true
